'use strict';

const TEXTURE_MAP_URI = '/textureMap',
      INDEX_PAGE_URI = '/',
      INDEX_PAGE_FILE_PATH = '/index.html',
      OVERLAY_TEXTURE_SIZE = 128;

module.exports = {
  TEXTURE_MAP_URI: TEXTURE_MAP_URI,
  INDEX_PAGE_URI: INDEX_PAGE_URI,
  INDEX_PAGE_FILE_PATH: INDEX_PAGE_FILE_PATH,
  OVERLAY_TEXTURE_SIZE: OVERLAY_TEXTURE_SIZE
};

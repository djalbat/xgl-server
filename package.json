{
  "name": "jiggles",
  "author": "James Smith",
  "version": "0.0.5",
  "license": "MIT",
  "homepage": "https://github.com/djalbat/Jiggles",
  "description": "Server side components and utilities for Jiggle.",
  "repository": {
    "type": "git",
    "url": "https://github.com/djalbat/Jiggles"
  },
  "dependencies": {
    "sharp": "^0.18.4",
    "necessary": "^2.5.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-react-jsx": "^6.7.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "chokidar-cli": "^1.2.0",
    "express": "^4.15.4"
  },
  "scripts": {
    "clean": "rm -rf ./lib",
    "babel": "babel ./es6 --presets=es2015 --out-dir=./lib",
    "babel-debug": "babel ./es6 --presets=es2015 --source-maps inline --out-dir=./lib",
    "build": "npm run clean && npm run babel",
    "build-debug": "npm run clean && npm run babel-debug",
    "watch": "npm run build && chokidar './index.js' './es6/**/*.js' -c 'npm run build'",
    "watch-debug": "npm run build-debug && chokidar './index.js' './es6/**/*.js' -c 'npm run build-debug'"
  }
}
